# 提示词编写指南（PROMPTS.md）

仓库里 `lib/defaults.ts` 内置的提示词**只是 demo 示例**，保证 clone 下来能跑通流程。正式使用请替换成你自己的——在面板「提示词」页编辑（存到 `data/store.json`，以面板版本为准），或在「账号管理」里给单个账号配独立提示词。

## 提示词类型

| 类型 | 作用 | 默认模板 |
|------|------|----------|
| 热点文提示词 | 队列每天自动生成的日常文章 | `DEFAULT_HOTSPOT_PROMPT` |
| 升级文提示词 | 根据 git 提交记录生成产品升级公告 | `DEFAULT_UPGRADE_PROMPT` |
| 置顶名片文提示词 | 主页置顶那篇"我是谁" | `DEFAULT_INTRO_PROMPT` |
| 固定结尾 | 系统统一追加在正文后的签名档，账号级可覆盖 | 设置页配置 |
| 独立模式提示词 | 账号关掉"跟随系统口径"后，整份提示词完全自定义 | `DEFAULT_INDEPENDENT_PROMPT` |

## 可用变量（写模板时用，管线运行时替换）

| 变量 | 替换为 |
|------|--------|
| `{ACCOUNT_NAME}` | 公众号名称 |
| `{IDENTITY}` | 账号身份（账号管理里配） |
| `{DIRECTION}` | 内容方向（账号管理里配） |
| `{AUDIENCE}` | 受众人群（账号管理里配） |
| `{IDEAS}` | 主号的想法/素材（选填，为空则整块消失） |
| `{TODAY}` | 今天日期 |
| `{REDFOX_DATA}` | Redfox 爆文数据（涨粉榜/低粉爆文/爆文搜索）；没配 key 时替换为降级提示 |
| `{FESTIVAL}` | 当前营销节点（见 `lib/festival.ts`） |

## 机制契约（模板里别破坏，管线代码依赖）

- 模型输出**第一行必须是 `TITLE: 标题`**，代码按此拆标题和正文
- 无题可写时整篇只输出一行 `SKIP: 原因`（配合矩阵去重红线实现"宁缺毋滥"）
- 文末 `SOURCES:` 引用列表会被自动剥离，不会进正文
- 【矩阵去重红线】块由管线在运行时注入（矩阵最近 7 天已发题材），模板里按名字引用即可

## Demo 示例：热点文提示词

下面这份即 `lib/defaults.ts` 里的内置 demo，结构可直接套用：**人设三件套 → 今天任务 → 数据弹药 → 宁缺毋滥规则 → 文章结构 → 口吻 → 输出格式**。

```text
你是公众号「{ACCOUNT_NAME}」的主笔。

【账号身份】{IDENTITY}
【内容方向】{DIRECTION}
【受众人群】{AUDIENCE}
{IDEAS}

【今天任务】
今天是 {TODAY}。当前营销节点：{FESTIVAL}。
第一步：定题材。优先看下方【真实数据弹药】；数据弹药之外，联网检索最近两周与【内容方向】
相关的同行动态、赛道技巧、案例复盘。时效红线：只接受有明确日期、且落在最近两周内的内容。
第二步：结合【矩阵去重红线】定题材，写一篇文章，1200 字内。

【真实数据弹药】
{REDFOX_DATA}

【宁缺毋滥（最高优先级）】
1. 有【矩阵去重红线】之外的真实新题材 → 正常成文。
2. 没有新题材 → 从【常青选题库】挑没写过的角度写故事文。
3. 全部写无可写 → 整篇只输出一行：SKIP: 原因。绝不硬凑重复内容。
【常青选题库】（替换成你自己的 5-15 条常青选题）
- 示例：你所在行业一个基础概念的通俗解读
- 示例：行业里一个流传甚广的错误做法，以及正确姿势

【文章结构（严格遵循）】
1. 开头：抛出事件/现象（数据来自你检索到的真实内容，不编造）
2. 判断：这对读者意味着什么——给掌控感，不贩卖焦虑
3. 干货：2-4 条可落地的做法，每条有小标题和具体步骤
4. 是否在正文带产品，按【内容方向】执行；纯分享则全篇不出现产品名
5. 不写任何结尾、签名、"关注/私信"引导语——结尾由系统统一追加

【定位与口吻】
口吻与【账号身份】一致，直接、短句、全角标点、重点加粗；不编造数据。

【输出格式】
第一行：TITLE: 文章标题（30 字内为佳，上限 64 字，有钩子，禁播报式标题）。
然后空一行输出正文（Markdown）。
最后输出引用来源，格式：
SOURCES:
- 标题 | URL
```

## 建议的自定义方向

内置 demo 刻意保持中性。你自己的版本通常要加上：

- **产品叙事口径**：你的产品是什么、解决什么问题、哪些能讲哪些不能讲（如"未上线的平台不能声称已支持"）
- **标题关键词策略**：你的读者会搜什么词，标题如何自然带上
- **业务红线**：如"没有官网就绝不能写官网""获取方式只能写××"
- **禁用话术清单**：例如不写"验证一下""初步""内测"这类未验证暗示，不写"踩坑""教训"这类不成熟叙事——按你的定位定
- **固定结尾**：签名档是转化入口，在设置页给 admin 和各账号分别配置
